# 알림 발송 설정 — 이 파일을 notify.yaml로 복사해 채우세요 (notify.yaml은 gitignore).
# 채널별로 값이 없으면 자동으로 건너뜁니다.

# 텔레그램: 비워두면 AI FACTORY MANAGER\.env 의 aifm 봇(TELEGRAM_BOT_TOKEN/CHAT_ID)을
#           자동 사용합니다. 다른 봇을 쓰려면 아래를 채우세요.
# telegram:
#   bot_token: "123456:ABC..."
#   chat_id: "123456789"
#   env_file: "C:\\Users\\jhsum\\code\\AI FACTORY MANAGER\\.env"   # 대체 .env 경로

# Gmail: 앱 비밀번호 필요 (일반 비밀번호 아님).
#   생성: Google 계정 → 보안 → 2단계인증 → 앱 비밀번호(16자리)
gmail:
  user: "jhsumin@gmail.com"          # 보내는 계정
  app_password: "여기에_16자리_앱비밀번호"   # 공백 없이
  to: "jhsumin@gmail.com"            # 받는 주소(여러개면 콤마)
